{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && prisma db seed && next build",
  "regions": ["gru1"]
}
